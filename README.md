# Numbers Station

A small experimental project that generates numbers-station-style audio
and hides messages within the output using audio steganography.

The Rust crate contains the core generation and encoding logic.
